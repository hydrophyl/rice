set filesep " "
# copy file path
map Y %echo %fx% | clip 
# open file in nvim
map V &nvim-qt %f%
map d # leave the command name empty like this for unmapping
map dd delete
map dD $IFS=':'; rm -rf $fx
